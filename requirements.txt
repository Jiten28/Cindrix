flask>=3.0
python-dotenv>=1.0
requests>=2.31
google-genai>=1.0
pypdf>=4.0
python-docx>=1.1
gunicorn>=21.0

# Knowledge-base RAG pipeline (see docs/Architecture.md "Retrieval, Vector
# Store & Guardrails" for why each was picked)
numpy>=1.26            # vector math for the RAG pipeline; also faiss's own dependency
faiss-cpu>=1.8          # real ANN vector store, replaces the old brute-force
                        # cosine loop in app/ai/embeddings.py — CPU-only, no
                        # server/infra to run, fits this project's
                        # no-external-service pattern. Soft-imported: code
                        # falls back to an exact numpy search over the index's
                        # .vectors.npy sidecar with a loud warning if this
                        # can't be imported, so the app still serves grounded
                        # answers (slower) either way — see
                        # app/rag/vector_store.py. NOTE: the wheel links
                        # libgomp.so.1, which Debian slim images don't ship;
                        # the Dockerfile installs libgomp1 for this reason.
huggingface_hub>=0.23   # downloads the target language's MSMARCO-XI parquet
                        # shard by name (hf_hub_download) — replaces the
                        # HuggingFace `datasets` library, which crashed with
                        # ArrowNotImplementedError on the dataset's nested
                        # `passages` struct. Soft-imported, see app/rag/dataset.py
pyarrow>=15             # reads that parquet shard into plain Python dicts
                        # (.to_pylist()) — handles the nested struct `datasets`
                        # couldn't. Also soft-imported, see app/rag/dataset.py
